{
  "name": "@my-game-shelf/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/main.ts",
    "build": "tsc --project tsconfig.json",
    "lint": "node -e \"console.log('lint not configured yet')\"",
    "typecheck": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "@my-game-shelf/contracts": "0.1.0",
    "@my-game-shelf/domain": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
